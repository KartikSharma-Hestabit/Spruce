package com.example.spruce.screens

enum class NavigationRoutes(route: String) {

    Home(route = "Home"),
    Post(route = "Post"),
    Category(route = "Category"),
    PostDetails(route = "PostDetail")

}